import java.util.Scanner
import java.util.Collections.max

fun main() {
    val reader = Scanner(System.`in`)
    var matrix = ArrayList<IntArray>(6)
    for (row in 0 until 6) {
        var theRow = reader.nextLine()
        var rowList = theRow.split(" ").toTypedArray().map { it.toInt() }
    }
}

fun findHourglassMax(matrix:ArrayList<IntArray>):Int{
    for (i in 0 until 4){
        for (j in 0 until 4){
            
        }
    }
    return 0
}
